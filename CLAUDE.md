# CLAUDE.md — AI セッションの入口

このファイルは**入口だけ**を示す。ルール・設計・現在地はここに複製しない。

## 最初に読むもの

```
prompts/vNext/STARTUP_PROMPT.md
```

**読込経路の正本はこのファイルである。** 何をどの順に読むか（Base 3 文書と、
着手内容に応じて追加で読む Overlay）はすべてそこに定義されている。
本ファイルの指示はここで終わりであり、以降は STARTUP_PROMPT に従うこと。

## 前提

- **会話ログは正本ではない。** 過去のセッションで決めたと言われた事項も、
  Repository のファイルに保存されるまでは正本として扱わない
- 断片から構造・ルール・値を推測補完しない
- 各文書の責務境界と authority は `docs/DEVELOPMENT_STANDARD.md` §6 / §7 が定める
